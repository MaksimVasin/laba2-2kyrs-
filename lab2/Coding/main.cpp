#include <iostream>
//#include "Linkedlist.h"
#include <string>
#include "Shannon-Fano.h"

using namespace std;

int main()
{
    print_to_console();

    /*string str, code, code2, str2;
    getline(cin, str);
    code = coding_string(str);
    cout << endl << code;

    getline(cin, code2);
    str2 = decoding_code(str, code2);
    cout << endl << str2;*/
    return 0;
}